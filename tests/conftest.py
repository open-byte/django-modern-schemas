"""Django for the test suite is configured by the repository-root ``conftest.py``.

Keeping it there lets the doctests under ``docs/`` run against the same settings,
so every example in the published documentation is executed by this suite.
"""
